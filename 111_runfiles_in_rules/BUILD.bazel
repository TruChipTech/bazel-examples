load("@rules_python//python:defs.bzl", "py_binary")
load(":launcher.bzl", "wrapped_binary")

py_binary(
    name = "inner",
    srcs = ["inner.py"],
    data = ["inner_data.txt"],
    main = "inner.py",
)

wrapped_binary(
    name = "wrapped",
    binary = ":inner",
    config = "settings.conf",
)
