load("@rules_python//python:defs.bzl", "py_binary")
load(":format.bzl", "formatted")

py_binary(
    name = "formatter",
    srcs = ["formatter.py"],
    main = "formatter.py",
    visibility = ["//visibility:public"],
)

# The caller never mentions the formatter tool - the rule supplies it.
formatted(
    name = "pretty",
    src = "input.txt",
)
