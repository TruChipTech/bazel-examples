# No targets needed - this sample is about the `bazel mod` command family.
genrule(
    name = "placeholder",
    outs = ["note.txt"],
    cmd = "echo 'see README.md - this sample is about bazel mod' > $@",
)
